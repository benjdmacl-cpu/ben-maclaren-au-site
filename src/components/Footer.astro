<footer>
	<div class="wrap footer-inner">
		<div class="footer-top">
			<div class="footer-group">
				<p>Ben Maclaren — Counsellor</p>
				<p>Canberra, ACT</p>
			</div>
			<div class="footer-group footer-meta">
				<p>ABN 50 228 140 416</p>
				<p>ACA Membership No. S84574</p>
			</div>
		</div>
		<nav class="footer-sitemap" aria-label="Site pages">
			<a href="/">Home</a>
			<a href="/journal">Journal</a>
			<a href="/resources">Resources</a>
			<a href="/privacy">Privacy &amp; Confidentiality</a>
		</nav>
	</div>
</footer>

<style>
	footer {
		min-height: 20vh;
		display: flex;
		align-items: center;
		padding: var(--space-8) 0;
		background: var(--color-bg-dark);
		border-top: var(--border-hairline) solid var(--color-border-on-dark);
	}

	.footer-inner {
		width: 100%;
		display: flex;
		flex-direction: column;
		gap: var(--space-8);
	}

	.footer-top {
		display: flex;
		justify-content: space-between;
		align-items: center;
		flex-wrap: wrap;
		gap: var(--space-2-5) var(--space-8);
	}

	.footer-group {
		display: flex;
		flex-direction: column;
		gap: var(--space-1);
	}

	.footer-meta {
		text-align: right;
	}

	footer p {
		font-family: var(--font-mono);
		font-size: var(--fs-meta-label);
		letter-spacing: var(--ls-tight-label);
		color: var(--color-text-muted-on-dark);
		margin: 0;
	}

	footer a {
		color: inherit;
		text-decoration: none;
	}

	footer a:hover {
		color: var(--color-accent-on-dark);
	}

	.footer-sitemap {
		display: flex;
		flex-wrap: wrap;
		gap: var(--space-2-5) var(--space-6);
		padding-top: var(--space-6);
		border-top: var(--border-hairline) solid var(--color-border-on-dark);
	}

	.footer-sitemap a {
		font-family: var(--font-mono);
		font-size: var(--fs-meta-label);
		letter-spacing: var(--ls-tight-label);
		text-transform: uppercase;
		color: var(--color-text-muted-on-dark);
	}

	@media (max-width: 480px) {
		.footer-top {
			flex-direction: column;
			align-items: flex-start;
		}

		.footer-meta {
			text-align: left;
		}
	}
</style>
