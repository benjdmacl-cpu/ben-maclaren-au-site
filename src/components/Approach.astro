<section class="approach" id="approach">
	<div class="wrap">
		<div>
			<span class="eyebrow">Approach</span>
			<h2>Find what works</h2>
		</div>
		<div class="approach-body">
			<p>
				Together, we set meaningful goals and experiment with what might help, keeping what the evidence
				shows actually works for you.
			</p>
			<p><em>What therapy methods do I use?</em></p>
			<p>
				I draw from a wide range of methods, always looking for new tools to add to the kit, with a
				personal focus on Rogerian, ACT and Gestalt therapy.
			</p>
		</div>
	</div>
</section>

<style>
	.approach {
		padding: var(--space-27-5) 0;
		background: var(--color-bg-card);
	}

	.approach .wrap {
		display: grid;
		grid-template-columns: 0.85fr 1.15fr;
		gap: var(--space-20);
		align-items: start;
	}

	.approach .eyebrow {
		margin-bottom: var(--space-5-5);
		display: block;
	}

	.approach h2 {
		font-size: var(--fs-display);
		line-height: var(--lh-article-title);
	}

	.approach-body p {
		font-size: var(--fs-prose-body);
		color: var(--color-text-secondary);
		margin: 0 0 var(--space-5);
	}

	.approach-body p:last-child {
		margin-bottom: 0;
	}

	.pull {
		font-family: var(--font-display);
		font-style: italic;
		font-size: var(--fs-prose-quote);
		color: var(--color-text-primary);
		border-left: var(--border-strong) solid var(--color-accent-link);
		padding-left: var(--space-5-5);
		margin: var(--space-8) 0;
	}

	@media (max-width: 820px) {
		.approach .wrap {
			grid-template-columns: 1fr;
			gap: var(--space-9);
		}
	}
</style>
