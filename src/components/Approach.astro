<section class="approach" id="approach">
	<div class="wrap">
		<div>
			<span class="eyebrow">Approach</span>
			<h2>Find what works</h2>
		</div>
		<div class="approach-body">
			<p>
				I don't bring a fixed method to every session. I listen, we explore what's going on together, and
				experiment with what might help — keeping what the evidence shows is actually working for you.
			</p>
			<p>What therapy methods do I use?</p>
			<p>I draw widely with a personal focus on Rogerian and Gestalt Therapy.</p>
		</div>
	</div>
</section>

<style>
	.approach {
		padding: 110px 0;
		background: var(--cream);
	}

	.approach .wrap {
		display: grid;
		grid-template-columns: 0.85fr 1.15fr;
		gap: 80px;
		align-items: start;
	}

	.approach .eyebrow {
		margin-bottom: 18px;
		display: block;
	}

	.approach h2 {
		font-size: 34px;
		line-height: 1.15;
	}

	.approach-body p {
		font-size: 16.5px;
		color: var(--ink-soft);
		margin: 0 0 20px;
	}

	.approach-body p:last-child {
		margin-bottom: 0;
	}

	.pull {
		font-family: 'Fraunces', serif;
		font-style: italic;
		font-size: 22px;
		color: var(--ink);
		border-left: 2px solid var(--terracotta);
		padding-left: 22px;
		margin: 32px 0;
	}

	@media (max-width: 820px) {
		.approach .wrap {
			grid-template-columns: 1fr;
			gap: 36px;
		}
	}
</style>
