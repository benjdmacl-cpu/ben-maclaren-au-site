<nav>
	<div class="wrap">
		<a class="nav-name" href="/">Ben Maclaren</a>
		<!-- Resources and Journal links hidden from nav for now — reactivate
		     later by uncommenting. The pages themselves are untouched. -->
		<!-- <div class="nav-links">
			<a href="/resources">Resources</a>
			<a href="/journal">Journal</a>
		</div> -->
		<a
			class="nav-cta"
			href="https://www.halaxy.com/book/benjamin-maclaren/counsellor/1780895/1362507"
			target="_blank"
			rel="noopener noreferrer"
			data-goatcounter-click="nav-book-intro-call">Book an intro call</a
		>
	</div>
</nav>

<style>
	nav {
		position: relative;
		z-index: 3;
		padding: var(--space-6-5) 0;
		border-bottom: var(--border-hairline) solid var(--color-border);
	}

	nav .wrap {
		display: flex;
		align-items: center;
		justify-content: space-between;
	}

	.nav-name {
		font-family: var(--font-display);
		font-size: var(--fs-brand);
		font-weight: var(--fw-semibold);
		letter-spacing: var(--ls-brand);
		text-decoration: none;
	}

	.nav-links {
		display: flex;
		gap: var(--space-8-5);
		font-family: var(--font-mono);
		font-size: var(--fs-caption);
		letter-spacing: var(--ls-caps);
		text-transform: uppercase;
		color: var(--color-text-secondary);
	}

	.nav-links a {
		text-decoration: none;
	}

	.nav-links a:hover {
		color: var(--color-accent);
	}

	.nav-cta {
		font-family: var(--font-mono);
		font-size: var(--fs-caption);
		letter-spacing: var(--ls-label);
		text-transform: uppercase;
		text-decoration: none;
		color: var(--color-on-accent);
		background: var(--color-accent-solid);
		padding: var(--space-2-5) var(--space-4-5);
		border-radius: var(--radius-sm);
		transition: background 0.15s ease;
	}

	.nav-cta:hover {
		background: var(--color-accent);
	}

	@media (max-width: 720px) {
		nav .wrap {
			flex-wrap: wrap;
			row-gap: var(--space-3);
		}

		.nav-name {
			order: 1;
		}

		.nav-cta {
			order: 2;
		}

		.nav-links {
			order: 3;
			flex-basis: 100%;
			padding-top: var(--space-3);
			border-top: var(--border-hairline) solid var(--color-border);
		}
	}
</style>
