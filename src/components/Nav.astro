<nav>
	<div class="wrap">
		<a class="nav-name" href="/">Ben Maclaren</a>
		<div class="nav-links">
			<!-- Resources hidden from nav for now — reactivate later by
			     uncommenting. The /resources page itself is untouched. -->
			<!-- <a href="/resources">Resources</a> -->
			<a href="/journal">Journal</a>
		</div>
		<a class="nav-cta" href="/#contact">Book a conversation</a>
	</div>
</nav>

<style>
	nav {
		position: relative;
		z-index: 3;
		padding: var(--space-6-5) 0;
		border-bottom: var(--border-hairline) solid var(--color-border);
	}

	nav .wrap {
		display: flex;
		align-items: center;
		justify-content: space-between;
	}

	.nav-name {
		font-family: var(--font-display);
		font-size: var(--fs-brand);
		font-weight: var(--fw-semibold);
		letter-spacing: var(--ls-brand);
		text-decoration: none;
	}

	.nav-links {
		display: flex;
		gap: var(--space-8-5);
		font-family: var(--font-mono);
		font-size: var(--fs-caption);
		letter-spacing: var(--ls-caps);
		text-transform: uppercase;
		color: var(--color-text-secondary);
	}

	.nav-links a {
		text-decoration: none;
	}

	.nav-links a:hover {
		color: var(--color-accent);
	}

	.nav-cta {
		font-family: var(--font-mono);
		font-size: var(--fs-caption);
		letter-spacing: var(--ls-label);
		text-transform: uppercase;
		text-decoration: none;
		color: var(--color-on-accent);
		background: var(--color-accent-solid);
		padding: var(--space-2-5) var(--space-4-5);
		border-radius: var(--radius-sm);
		transition: background 0.15s ease;
	}

	.nav-cta:hover {
		background: var(--color-accent);
	}

	@media (max-width: 720px) {
		.nav-links {
			display: none;
		}
	}
</style>
