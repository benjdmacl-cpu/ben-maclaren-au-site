<section class="audiences" id="audiences">
	<div class="wrap">
		<span class="eyebrow">Where I focus</span>
		<h2>I work often with</h2>
		<div class="audience-grid">
			<div class="audience-card">
				<span class="icon" aria-hidden="true">
					<svg viewBox="0 0 90 90" width="48" height="48">
						<path d="M 14 64 L 36 30 L 52 50 L 64 34 L 78 64 Z" fill="none" stroke="currentColor"
							stroke-width="3.4" stroke-linejoin="round" stroke-linecap="round"></path>
					</svg>
				</span>
				<h3>Men</h3>
				<p>
					A lot of men come in stuck and not sure how they got there. Maybe it's anger that catches you
					off guard, a relationship that's stopped working, or a flatness you can't explain to anyone,
					including yourself. Maybe you were raised to handle things on your own, and that's stopped
					working too.
				</p>
				<p>
					There's no fixed idea here of what a man's supposed to be. We work with what's actually going
					on, including the stuff you haven't said out loud yet, and find a way of being honest with
					yourself that doesn't feel like weakness.
				</p>
			</div>
			<div class="audience-card">
				<span class="icon" aria-hidden="true">
					<svg viewBox="0 0 90 90" width="48" height="48">
						<path
							d="M 27 45 C 27 30, 42 30, 45 45 C 48 60, 63 60, 63 45 C 63 30, 48 30, 45 45 C 42 60, 27 60, 27 45 Z"
							fill="none" stroke="currentColor" stroke-width="3.4" stroke-linecap="round"></path>
					</svg>
				</span>
				<h3>Neurodivergent</h3>
				<p>
					Whether you've just been diagnosed or you've known for years, we start with exploring and
					understanding how your mind actually works, not how it's "supposed" to work. Together, we
					experiment with ways of doing and being that actually fit you.
				</p>
				<p>
					I bring direct experience with ADHD, autism, and other presentations, and know what it takes
					to find your own way of working with yourself.
				</p>
			</div>
			<div class="audience-card">
				<span class="icon" aria-hidden="true">
					<svg viewBox="0 0 90 90" width="48" height="48">
						<path d="M 20 62 C 32 62, 32 43, 44 43 C 56 43, 56 24, 68 24" fill="none"
							stroke="currentColor" stroke-width="3.4" stroke-linecap="round"></path>
						<path d="M 59 19 L 68 24 L 64 34" fill="none" stroke="currentColor" stroke-width="3.4"
							stroke-linecap="round" stroke-linejoin="round"></path>
					</svg>
				</span>
				<h3>Life and career transitions</h3>
				<p>
					Changing careers, rebuilding direction, working out what matters now. A lot of us are finding
					new meaning in a faster-changing world, realising we don't like the way things are, or
					searching for a calling that actually fits.
				</p>
				<p>
					Support here is about finding your way through the limbo and building a life worth living.
				</p>
			</div>
		</div>
	</div>
</section>

<style>
	.audiences {
		padding: var(--space-22-5) 0;
		border-top: var(--border-hairline) solid var(--color-border);
		border-bottom: var(--border-hairline) solid var(--color-border);
		background: var(--color-bg-page);
	}

	.audiences .eyebrow {
		margin-bottom: var(--space-3-5);
		display: block;
	}

	.audiences h2 {
		font-size: var(--fs-section-heading);
		margin-bottom: var(--space-13);
		max-width: 560px;
	}

	.audience-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--space-10);
	}

	.audience-card .icon {
		display: block;
		margin-bottom: var(--space-5-5);
		color: var(--color-accent);
	}

	.audience-card h3 {
		font-size: var(--fs-card-title);
		margin-bottom: var(--space-3);
	}

	.audience-card p {
		color: var(--color-text-secondary);
		font-size: var(--fs-card-body);
		margin: 0 0 var(--space-3);
	}

	.audience-card p:last-child {
		margin-bottom: 0;
	}

	@media (max-width: 720px) {
		.audience-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
