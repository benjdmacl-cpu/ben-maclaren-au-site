<section class="hero">
	<svg class="hero-motif" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
		<circle cx="100" cy="100" r="6" fill="none" stroke-width="1.4"></circle>
		<circle cx="100" cy="100" r="20" fill="none" stroke-width="1.4"></circle>
		<circle cx="100" cy="100" r="38" fill="none" stroke-width="1.2" opacity="0.75"></circle>
		<circle cx="100" cy="100" r="60" fill="none" stroke-width="1" opacity="0.5"></circle>
		<circle cx="100" cy="100" r="86" fill="none" stroke-width="0.8" opacity="0.3"></circle>
	</svg>
	<div class="wrap hero-inner">
		<span class="eyebrow">Counsellor</span>
		<h1>A steady place<br />to work things out.</h1>
		<p class="tagline">Supporting a life worth living, through mental health.</p>
		<div class="hero-ctas">
			<a class="btn-primary" href="/#contact">Book a session</a>
			<a class="btn-secondary" href="/#contact">Book an intro call</a>
		</div>
		<a class="hero-contact-line" id="hero-email-link" href="#contact">Email</a>
	</div>
</section>

<style>
	.hero {
		position: relative;
		padding: var(--space-30) 0 var(--space-32-5);
		overflow: hidden;
	}

	.hero-motif {
		position: absolute;
		top: -60px;
		right: -60px;
		width: 420px;
		height: 420px;
		opacity: 0.55;
		z-index: 0;
		color: var(--terracotta);
	}

	.hero-motif circle {
		stroke: currentColor;
	}

	.hero-inner {
		position: relative;
		z-index: 2;
		max-width: 640px;
	}

	.hero .eyebrow {
		margin-bottom: var(--space-5-5);
		display: block;
	}

	.hero h1 {
		font-size: var(--fs-hero);
		line-height: var(--lh-hero);
		letter-spacing: var(--ls-hero-tight);
		margin-bottom: var(--space-6-5);
	}

	.hero p.tagline {
		font-family: var(--font-display);
		font-style: italic;
		font-weight: var(--fw-regular);
		font-size: var(--fs-tagline);
		color: var(--color-text-secondary);
		max-width: 480px;
		margin: 0 0 var(--space-10);
	}

	.hero-ctas {
		display: flex;
		align-items: center;
		gap: var(--space-6-5);
		flex-wrap: wrap;
	}

	.btn-primary {
		font-family: var(--font-mono);
		font-size: var(--fs-caption);
		letter-spacing: var(--ls-label);
		text-transform: uppercase;
		text-decoration: none;
		color: var(--color-on-accent);
		background: var(--color-accent-solid);
		padding: var(--space-4) var(--space-7-5);
		border-radius: var(--radius-sm);
		transition:
			background 0.15s ease,
			transform 0.15s ease;
		display: inline-block;
	}

	.btn-primary:hover {
		background: var(--color-accent);
		transform: translateY(-1px);
	}

	.btn-secondary {
		font-family: var(--font-mono);
		font-size: var(--fs-caption);
		letter-spacing: var(--ls-label);
		text-transform: uppercase;
		text-decoration: none;
		color: var(--color-accent-solid);
		background: transparent;
		border: var(--border-hairline) solid var(--color-accent-solid);
		padding: calc(var(--space-4) - var(--border-hairline)) calc(var(--space-7-5) - var(--border-hairline));
		border-radius: var(--radius-sm);
		transition:
			background 0.15s ease,
			color 0.15s ease;
		display: inline-block;
	}

	.btn-secondary:hover {
		background: var(--color-accent-solid);
		color: var(--color-on-accent);
	}

	.hero-contact-line {
		font-family: var(--font-mono);
		font-size: var(--fs-caption);
		color: var(--color-text-secondary);
		text-decoration: none;
		border-bottom: var(--border-hairline) solid var(--color-border);
		padding-bottom: var(--space-0-5);
		display: inline-block;
		margin-top: var(--space-6-5);
	}

	.hero-contact-line:hover {
		border-color: var(--color-accent-link);
		color: var(--color-accent);
	}
</style>

<script>
	// Same rationale as Contact.astro's email link — base64 + atob() so the
	// address is assembled at runtime and never appears as literal text in
	// the built HTML/JS for scrapers to find.
	const link = document.getElementById('hero-email-link');
	if (link instanceof HTMLAnchorElement) {
		const address = atob('aGVsbG9AYmVubWFjbGFyZW4uY29tLmF1');
		link.href = `mailto:${address}`;
		link.textContent = address;

		link.addEventListener('click', (e) => {
			e.preventDefault();
			navigator.clipboard?.writeText(address);
			const original = link.textContent;
			link.textContent = 'Copied!';
			setTimeout(() => {
				link.textContent = original;
			}, 1500);
		});
	}
</script>
